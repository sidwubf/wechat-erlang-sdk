%%% -*- mode: erlang -*-
{erl_opts, [debug_info,
            {i, "test"},
            {src_dirs, ["src"]}]}.

{eunit_opts, [verbose, {report, {eunit_surefire, [{dir, "."}]}}]}.

{cover_enabled, true}.

{deps, [
    {gun, ".*", {git, "https://github.com/extend/gun.git", {branch, master}}}
]}.
